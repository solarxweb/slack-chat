{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "diploma thesis, chat slack",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "start-server -s ./frontend/dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chat",
    "slack"
  ],
  "author": "D. Romanenko",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.4"
  }
}
